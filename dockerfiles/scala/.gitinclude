# I'm here too! Place a `scala3-library_3-3.1.1.jar` here! (Note: "Dockerfile" will look for `scala3-library.jar`)
# I ran `find / -name scala3*-library* 2>/dev/null` to find where mine was located instead of downloading a new one.